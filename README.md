## HW1
### P1

Run example of nonlinear regression with numpy only (no torch).

From the project root run:
```
python3 -m HW1.P1.examples.regression_example
```

Run example of classification with three classes in 2D (for ease of visualization) 
with numpy only (no torch).

From the project root run:
```
python3 -m HW1.P1.examples.classification_example
```

The classification example will not converge, since there are overlapping samples
over the three classes.